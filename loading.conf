{
    instances = {
        byfilename = DataReader(f"queries/GetFile.sql", connection = "agfiles") {
            none->HttpStatus(404) {
                next->Translation(f"templates-%s/404.html", locales = ["en", "nl"]);
            };
            single->ReadFromFile(
                pathvariable = "path",
                rootpath = f"storage",
                isrelative = true
            ) {
                header->HttpContent();
            };
        };

        byfilenamecached = ContextCache(lifetime = "2s", keyname = "filename") {
            begin->instances.byfilename;
        };
    };
}
