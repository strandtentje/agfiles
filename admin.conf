{
    instances = {
        begin = Dir("filename") {
            main->HttpMethod() {
                get->Translation(f"templates-%s/list.html", locales = ["en", "nl"]) {
                    items->DataReader(f"queries/Get File.auto.sql", connection = "agfiles") {
                        iterator->Translation(f"templates-%s/item.html", locales = ["en", "nl"]);
                    };
                };
            };
        };
    };
}